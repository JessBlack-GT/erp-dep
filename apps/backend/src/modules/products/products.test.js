/**
 * ERP-SYSTEM - Test de products
 */
const { expect } = require('chai');
describe('products Module', () => {
  it('deberia tener la estructura preparada', () => { expect(true).to.be.true; });
});
