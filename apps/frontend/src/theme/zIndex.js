/**
 * ============================================
 * YJ NEXO ERP - zIndex Tokens
 * ============================================
 */

export const zIndex = {
  base: 0,
  sticky: 100,
  dropdown: 200,
  overlay: 300,
  modal: 400,
  toast: 500,
  tooltip: 600,
};

export default zIndex;
